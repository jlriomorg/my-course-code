name: Checkout
on: [push]

jobs:
  checkout-and-display-files:
    runs-on: ubuntu-latest
    steps:
      - name: List files Before
        run: ls-a
      - name: Checkout
        run: |
          git init
          get remote add origin "https://$GITHUB_ACTOR:${{secrets.GITHUB_TOKEN}}"